services:
  - type: web
    name: docuseal-wholesale-ai
    env: docker
    dockerfilePath: infra/docuseal-render/Dockerfile
    plan: starter
    region: oregon
    disk:
      name: docuseal-data
      mountPath: /data
      sizeGB: 10
